source "https://rubygems.org"

# Matches what GitHub Pages runs server-side, so local preview == deployed site.
gem "github-pages", group: :jekyll_plugins

# Ruby 3.x no longer ships webrick, which `jekyll serve` needs.
gem "webrick", "~> 1.8"
